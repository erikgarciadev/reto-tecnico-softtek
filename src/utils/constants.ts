export const PHONE_NUMBER = "(01) 411 6001";

export const PERCENTAGE_DISCOUNT_FOR_SOMEONE = 0.05;

export const TYPES_BADGES = Object.freeze({
  DEFAULT: "default",
  PLAN: "plan",
});

export const TYPES_OPTIONS = Object.freeze({
  FOR_ME: "for_me",
  FOR_SOMEONE: "for_someone",
});

export const PATHS = Object.freeze({
  HOME: "/",
  PLANS: "/plans",
  SUMMARY: "/summary",
});
